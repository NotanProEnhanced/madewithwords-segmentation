# Admin Studio login — copy this file to `admin_studio.env` and set your own values.
# The real admin_studio.env is git-ignored and stays only on your machine.
ADMIN_USER=admin
ADMIN_PASS=change-me-to-something-long

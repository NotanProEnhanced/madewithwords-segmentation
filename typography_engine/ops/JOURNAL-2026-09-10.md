# 2026-09-10 — the v2 pet engine, checkpointed

13 commits, `366ed91` → `c835642`, on `claude/pet-engine-v2` (branched from `ops-consolidate`).
Tagged `checkpoint-2026-09-10`. Running on staging only, behind `PET_ENGINE=v2`; no live brand
has it. Every number below was measured, not estimated.

---

## What it is

A second pet renderer, `app/pet_v2/`, reached from the same call sites as the first through
`pet_proto.render_pet_portrait_dispatch`. With `PET_ENGINE` unset or anything but `v2`, the
first engine runs and the live brands are byte-identical to before this branch existed.

The animal is constructed from typography rather than a photograph with text laid over it:
streamlines follow the fur direction; each eye, the nose leather, the philtrum, the nostrils
and the mouth crease are drawn by dedicated passes from what is actually in the photo; the
silhouette sheds flyaway hairs; every remaining gap is filled down to the glyph floor. Letters
carry the photo's own colour; opacity carries tone. A type-only likeness score (face-weighted
SSIM of the blurred typography against the blurred source, no photo underneath) is printed
per render and was the judge for every change.

## The claim, and the numbers behind it

`PET_V2_VERBOSE=1` prints a `CLAIM METRICS` line per render. On the staging senior-dog upload:

    footprint 85.6% of the animal · exposed space 0.9% · 10,618 words
    letter-body collisions 1.08% · type-only likeness 0.572

On the four development photos at 1050px (`c835642`): black lab 0.618, tan dog 0.459,
doodle 0.408, cat 0.401; collisions 0.6–0.9%; exposed space 1.8–2.8%.

## What was found

**One size per streamline hid the whole hierarchy.** Each line took the average of the size
field over its full length. On a close-up a line runs cheek to frame edge, so every line
averaged to the same value: median 6px in every distance band. Words are now sized at their own
position. `945eb5a`.

**The "keep it fine" zone was the likeness weight map.** Its face ellipse is 1.8 eye-separations
wide. On a close-up it covered half the animal and 80% of structural words. Replaced by an
anatomical zone: eye discs, the fitted nose leather, a mouth band, with a short ramp. `945eb5a`,
widened for the eyes in `c835642`.

**Lanes were spaced for fine type.** Larger words on the far body collided and were rejected;
6px channel letters took the ground (120 structural words against 901 channel letters in the
far band). Lane spacing now floors at 0.85x the local type size. `945eb5a`.

**Distance from the face was distance from between the eyes.** On a close-up the nose is a full
eye-separation from that point, so the muzzle got the largest words in the frame. Distance is
now to the nearest feature, eye or nose, split into an anatomical near part (eye-separation
units) and a frame-relative far part. `c835642`.

**The engine kept per-render state in module globals.** Two renders overlapping on the service
threads shared one placement log; staging surfaced it to the customer as
`boolean index did not match indexed array ... 8767 vs 8761`. All per-render state is now
thread-local. Concurrent renders verified byte-identical to sequential. `a373443`.

**The backdrop choice was accepted and ignored**, and every request was a full render: preview,
loupe and each backdrop toggle overlapped and the third passed the browser's 120s limit. Now one
render per (photo, words, aspect) at the loupe height, cached; the loupe is a resize, a
backdrop toggle is arithmetic on the outer-ground weight, a duplicate request waits for the
render in flight. Measured: Gallery Gray 0.1s, print 64s, preview after print 0.1s. `12585cc`.

**Curvature-adaptive word length failed twice** (one long word dominated). Reverted to plain
round-robin; noted in the code.

## To deploy

    cd /root/typortrait-prod && git fetch origin claude/pet-engine-v2 && git checkout c835642
    cd /root/typortrait-stg/typography_engine
    TAG=$(SRC=/root/typortrait-prod ./ops/build-image.sh c835642) && echo "TAG=$TAG"
    ./ops/promote.sh typortrait-stg "$TAG" && ./staging.sh ps

An empty `TAG` means the build failed; do not promote. Production would be the same steps
against `typortrait-pawsinwords` with `PET_ENGINE=v2` in that tree's `.env`, after a soak.

Knobs: `PET_ENGINE`, `PET_V2_VERBOSE`, `PET_V2_MAX_RENDER_PX` (default 2400), `GOP_MAX_OVERLAP`
(default 0.08), `PET_V2_CACHE_ENTRIES` (default 4), `PET_V2_KEEP_FIELDS` (measurement only).

## What remains

1. The site's Small/Medium/Large type slider (`type_scale`) is accepted and ignored by v2.
2. `pet_landmarks.py` (RTMPose AP-10K) is not wired to the `landmarks` argument; the heuristic
   eye/nose detector is doing the work and has been right on every test photo so far.
3. The print render for `/download` runs inline (64s at 2400px); it should be a background job
   behind the "preparing files" state.
4. Type-only likeness fell 0.02–0.035 with the finer features in `c835642`; the customer's eye
   on staging decides whether that trade stands.
5. A regression gate through `ops/render-testset.sh` for the pet set, then the production
   promote.

The measurement scripts used here (`size_bands.py`, `zone_sizes.py`, `regset.py`) lived in the
session scratchpad and are not in the tree; the engine exposes what they need
(`_PLACEMENTS`, `_PLACEMENT_PASS`, `_FIELDS` with `PET_V2_KEEP_FIELDS=1`).

---

## Afternoon: the gate, and three fixes through it

`dc242ea` → `b1466a0`, tagged `checkpoint-2026-09-10b`. Staging is on `b1466a0`.

### The pet set exists

Nine photos at `/root/typortrait-testset/pets/src/` (tabby, doodle, collie, samoyed, old dog,
corgi, shepherd, black lab, senior close-up), chosen to vary framing as much as subject.
`ops/render-petset.sh` renders them through the container and files each render with the
engine's own report card; `ops/pet-gate.sh` compares two runs and fails on a threshold. Both
verified against a local instance first. Two harness lessons cost an hour each and are now
built in: a run is named after the image the container is actually running, not the tree's
HEAD (a tree at one commit with a container still on another filed baseline renders under
the new name and overwrote the real ones), and an existing run is never overwritten.

Baseline `dc242ea`, likeness: lab 0.615, old dog 0.568, samoyed 0.475, doodle 0.453, corgi
0.450, senior 0.444, tabby 0.414, shepherd 0.337, collie 0.332. Footprint 83.5–85.5%,
exposed 0.5–1.0%, collisions 0.81–1.20%.

### What the baseline pictures showed

The two full-body photos were not sellable. A glow traced round the whole silhouette, green
on the collie and white-gold on the backlit shepherd; the collie's black half was empty of
type; the shepherd's face was a smudge. The senior close-up was close, with a shadowed side
that read as photo.

### Fixes, each measured on a stand-in before it was deployed

**Silhouette glow, `0322f32` + `6dd2e21`.** Three causes. Edge pixels are part background
and were revealed through the letters and averaged into the gap colour: each pixel inside
the outline is now projected onto the line from local deep fur to local background and the
background share removed, in LAB chroma only (an RGB version turned the collie's white paws
violet; a luminance-weighted version read every lighter patch of fur as background). The
fringe hairs were fabricated: on every development photo the thin-line residual just outside
the matte is under 3 at the 75th percentile, so the 40–80 hairs per render had no evidence;
they are drawn only where a line really leaves the outline. Whisker spokes walked past the
outline on grass texture; outside the outline a spoke needs a stronger, unbroken line.
Stand-in: the tan dog with a deliberately loosened matte, inner-ring green shift −8.0 →
−0.7 a-units. Gate: eight pass, shepherd −0.0102 likeness against a 0.010 limit. Judged from
the picture as the metric losing credit for invented hairs, not a regression; recorded here
rather than by moving the threshold.

**Local coat mode, `b1466a0`.** One tonal mode per photo fails any two-tone or shadowed coat.
The coat luminance is read per pixel (masked blur, 1.5 base) and the mode chosen per region,
all three floor rules blended with soft weights. Stand-in: the tan dog with one half darkened
to black and the other lifted, which puts the mean where the collie's is. Black half, letters
vs nearby gaps: L 29 vs 9 with 27% of letters invisible → 49 vs 26 with 3%. Gate: nine pass,
every number identical (ink untouched). Pictures: the collie's black half is typography; the
senior's shadowed side carries words.

### What remains, in order

1. Resolution follows face size: full-body photos render with the face at a few hundred
   pixels and every feature pass at the six-pixel floor. Choose the working height so the
   eyes get enough pixels, up to the print cap; expect roughly double the render time on the
   shepherd. Collie, shepherd and corgi are the test cases.
2. Hero placement: a silhouette-distance term, so the name does not land across the outline
   (senior, bottom left).
3. Shepherd collisions sit at 1.01–1.20% against a 1.20% limit; the smallest subject at the
   font floor. Item 1 may resolve it.
4. Production promote after a soak: `PET_ENGINE=v2` in the pawsinwords tree's `.env`.

---

## 2026-09-14: faces on full-body photos, and the build to soak

`b1466a0` → `4dce903`, tagged `checkpoint-2026-09-14`. Staging is on `4dce903`. Gate chain:
`dc242ea` → `6dd2e21` → `b1466a0` → `8383eaf` → `4dce903`, every step rendered and compared.

### What the full-body pictures needed

The collie, shepherd and corgi had faces of two dots and a smudge. A stand-in (the tan dog at
45% in a frame of its own blurred background) showed the eye detector taking the ear tips as
eyes, and a fix went in for that (`332905c`: an eye-separation prior and a size cap in the
pair scorer, detector scales from the head band's width, and a re-render up to the print cap
when the eyes are under 170px apart). Deployed, it changed nothing: all nine renders were
byte-identical to `b1466a0`. The probe log showed why: on the real shepherd the heuristic
paired two dark patches on the shoulder and chest, 236px apart and 98px different in height.
It has no notion of where the head is on a full-body photo, and no stand-in built from a
head-and-shoulders crop could have shown that.

The fix was already in the repo. `pet_landmarks.py` (YOLOX + RTMPose AP-10K), verified on
every framing when it was written, had never been connected to v2. It now runs in the site's
entry point and hands eyes and nose to the engine (`8383eaf`). Shepherd: eyes level, 131px
apart, nose below; the 1.30x re-render fired; likeness 0.327 → 0.425. Eight of nine photos
moved up (lab 0.620 → 0.670), collisions fell on every one. The old dog fell 0.568 → 0.496 and
failed the gate: its landmarks were right, the score's anchors had moved, and the picture is
the best render of the whole effort. Recorded as a metric artefact; from here the anchors are
stable and the likeness column means what it says.

### Two more, then the soak build

Hero placement (`4dce903`): a hero line must clear the silhouette by the hero type's height.
Tan dog: 2 of 3 hero words crossed the outline → 1, 116px inside.

Determinism (`4dce903`): the shepherd rendered with 4,891 words in the gated run and 4,882 in
a probe. The shared glyph cache stored a bitmap built from the first call's exact angle and
opacity and reused it for every call in the same bin, so output depended on which photos had
warmed the cache (tan dog cold vs warm: 2705 vs 2769 words). Bitmaps are built from the bin
values now; cold and warm are byte-identical. Gate `8383eaf` → `4dce903`: nine pass, likeness
within ±0.006.

### Production

`4dce903` is the build to soak on staging. Production is `PET_ENGINE=v2` in the pawsinwords
tree's `.env` and the same build/promote; the first engine stays in the image as the fallback
and `PET_ENGINE` unset restores it. `PET_LM_DEBUG=1` is set in staging's `.env` and can stay.

### Open

- The collie's second eye sits in black fur and reads as a socket; the black lab's eyes are
  found by the model, so this is the eye pass on a black eye in black fur, not detection.
- Full-body previews take ~50 s (the face re-render), against 30-45 s for close-ups.
- The likeness score's blur scale follows the render height, so scores across re-rendered
  photos are not on one scale; fine for a gate, not for ranking photos against each other.

### Later on 2026-09-14: the three open items, `d369d5f`

The collie's shadowed eye: the darkest-blob fit sees fur and eye as one blob in black fur, or
returns a fur speck that passes the size checks. With a trusted landmark the fit must now be
eye-sized relative to the eye separation, sit on the point, and differ in tone from the fur
around it; otherwise the eye is built at the landmark from the size prior. Black lab: both eyes
built, likeness 0.664 → 0.675. Collie: the socket is now a small amber eye; likeness 0.353 →
0.340, recorded as new ink inside the face map, not a regression.

The wasted pass: with the model's eyes known before the engine starts, the face-size decision
moves to the entry point (7.4 of 20.7 s on the stand-in; ~3 s on staging's shepherd).

One likeness scale: every render is judged at 1000px tall. Four iterations stay: three saved
7 s and cost the black lab 0.017.

Gate `4dce903` → `d369d5f`: five identical, four moved, one flagged (the collie, above).
`d369d5f` is the soak build.

### Both eyes alike, `c7d1720`

Reported from the staging renders: on the black lab and the collie one eye came out pale and
glassy, the other dim, though the lab's are lit alike. Two causes on the development lab with
a trusted landmark pair: the eye renderer and the eye reveal decided the eye's geometry
separately (one had the landmark prior, the other only the dark-blob fit), and the coat-mode
reading blurred each eye's own iris and glint into the luminance around it, so one eye's
surroundings crossed the mid-coat threshold and the other's did not (L 95 vs 27 at the same
0.96 reveal). One `eye_geometry` decision for both; the eyes and nose left out of the coat
reading; half the photo's own tone kept inside the eye reveal. Staging: lab, collie and old
dog all show two matched eyes. `c7d1720` is the soak build.

### The soak, `0283d90` + `PIXEL_BUDGET`

Local, through the site's entry point, 38 cases: every slider, backdrop and aspect; a 240px
upload and a 3600x4500 one; greyscale, rotated, panorama, all-white, all-black, noise, PNG
with alpha, a room with no pet, a small face, a two-tone coat; empty words, one letter, fifty
words, a 36-letter word, non-ASCII, punctuation, lowercase; three photos at once and the same
photo three times at once. 37 pass. The one failure is garbage bytes, which raises the
ValueError the site turns into a 400 -- correct. Two findings:

- A 2400x700 panorama took 146 s for the preview: the working height alone let the padded
  4:5 canvas grow to 5,500 x 6,900px. The working resolution now carries a pixel budget as
  well as a height (a 4:5 canvas at that height); an ordinary portrait is untouched, the
  panorama renders in 68 s.
- The 2400px print took 98 s here against 64 s a week ago. It runs off the request now, so
  no customer waits on it, but the engine has grown heavier and this is worth a profile.

Staging: `ops/soak-petset.sh` runs the same matrix over the nine-photo set on the container,
with the concurrent cases, and lists failures, slow renders and threshold breaches.

### Speed, `947b0c6`

Profiled a 1600px preview: 706,000 scalar numpy clamps in the per-word and per-point closures
(9 s), 21,926 text bitmaps rendered for ~6,700 distinct word/size/alpha triples, and 256
quantiles of a 2-3 million pixel layer taken 21 times inside the tonal-match bisection
(3.4 s). Plain min/max; the unrotated text cached per triple; a fixed-stride subsample for
the quantiles. Engine A/B on one box: 43.0 → 31.5 s, ink byte-identical. Staging: tabby 46 → 31
s, doodle 50 → 34, collie 31 → 22. Gate `c7d1720` → `947b0c6`: nine moved by the quantile
table, every number identical. Staging soak (QUICK): 22 requests, none failed; two previews
at once share the box's cores and each take twice as long (100 s), which is capacity: on a
two-core box a render concurrency of one serves the first customer in 50 s instead of both in
100. `947b0c6` is the soak build.

### The full soak, `947b0c6` on staging

Nine photos x three slider settings x three aspects x two backdrops, plus the concurrent cases:
166 requests, none failed, none over 90 s, exposed space under 3.0% everywhere. Fresh renders
18.5 / 27.8 / 66 s (min / median / max, the 66 s the second of two at once). Every backdrop
toggle came from the cache in about a second. Capacity checked: the box has two cores and
`TYPO_RENDER_CONCURRENCY` is unset in both trees, so each container already queues renders one
at a time; nothing to set, and a soak on staging shares cores with production.

One finding. Letter-body collisions at the site's default (Small, 0.30) stay under the 1.20%
claim line on all 54 renders. At Medium two of 54 cross it (1.22, 1.40), at Large eight of 27
(1.23 to 1.70, the lab on a landscape canvas the worst). All on Gallery Dark, since the mid
backdrop reuses the same ink. Bigger words on the same footprint collide more; the collision
cap in the placer is per word and does not tighten with the size slider. Not a broken picture,
half a percent of touching letters, and a customer chooses Large; but it is the one row of the
matrix the claim does not yet hold for. Open item: make the placer's overlap cap scale with
the slider, then gate Large the way Small is gated. Production goes to `947b0c6`.

### 2026-09-15: a person or two pets on the pet site

Production went to `947b0c6` after the soak. Before it did, the question was what the engine
does with a human, or with two pets. Read from the code, then measured.

**What it did.** The page sends the pet flag for every upload, so there is no species check
and the human brands' "we couldn't find a face" gate is skipped on purpose. A person is matted
cleanly, the landmark model (cats and dogs only) declines, the dark-blob eye finder pairs
whatever it pairs, and the customer is told nothing. Two pets: the landmark model took the
largest box and one face; the second pet got no eye or nose pass, and because type grows with
distance from the one face, the largest words in the frame landed across its eyes (measured
on a two-lab composite: "LOYAL" and "BEAR" across the second muzzle, both its eyes dull). One
correction to what I first wrote: a second pet standing apart was NOT painted over. The
matte's largest-component rule governs only the solid fill; the soft matte is kept, so the
second animal stayed, minus the fill that keeps a light coat from dropping out.

**The warning.** The detector now runs on its own (`pet_landmarks.detect_subjects`), and the
entry point reports what it saw through a `notices` list that main.py turns into an
input-stage warning, the channel the page already shows under the preview: no cat or dog
("we couldn't spot a cat or dog in this photo, so the eyes and nose are our best guess"); a
pet whose face the model could not read; two pets with only one face read. Nothing is said
when the detector could not run. Notices are cached with the render, so the backdrop toggle
says the same thing the first render did.

**Two pets.** `comparable_subjects` keeps every box at least a quarter of the largest's area
(`PET_LM_SECOND_MIN`); each gets its own pose reading (`all_face_landmarks`). The landmark
string carries further faces after "|". In the engine every further face gets the eye, nose,
mouth and muzzle passes at its own eye separation, its own fine zone, its own eye and nose
reveal, its own whisker region clipped to its own eye line, the same weight in the likeness
map and the ink-priority map, and a place in the feature-distance rule, so type is fine on
both faces and grows away from both. The smallest face sets the working resolution. The
matte is solidified with every subject's box in hand, so a second pet standing apart gets the
solid fill too; with one subject `keep_boxes` is None and the matte is the one the gate was
measured on. The first face remains the primary for hero placement and the region map.

**A bug the composite found.** The first two-face render left an 11% bare patch beside the
second dog: streamline growth is one seed and its descendants, and it crosses a narrow bridge
(two chests touching) only if a seed happens to land on it -- one landmark configuration
crossed, the other did not -- and it can never reach a pet standing apart. Growth now re-seeds
any unreached territory a good three lanes from everything grown so far, for the
portrait-wide call only (the pocket, channel and micro-fill callers keep their single wave).
A single pet never has such territory: the hero dog through the site path and the lab
through the engine with landmarks are byte-identical before and after every change here.

**Measured, two-lab composite (the lab and its mirror), same words and mask:**

    one face   likeness 0.4312  exposed 0.4%  collisions 0.99%  footprint 85.5%
    two faces  likeness 0.4839  exposed 0.5%  collisions 0.91%  footprint 86.3%
    apart      likeness 0.4013  exposed 0.2%  collisions 1.25%  footprint 86.6%   (240px gap)

Both eyes on both dogs lit and revealed, fine type on both faces. The apart case is over the
1.20% collision line by a twentieth; a composite, not a photo, and noted rather than tuned.

**Not yet measured.** The detector and pose model do not run in this sandbox, so the
detection, the second reading and the notices are exercised on staging. The pet set has no
two-pet photo and no person; `ops/TESTSET.md` says what to add and what to look for.

### 2026-09-15, 03:04: production went down, and why

`947b0c6` was promoted to pawsinwords after the soak. The first customer-style render timed
out in the browser, the retry got a network error, ssh timed out, and the box needed a
restart from the provider's panel. The previous boot's journal has the cause: the kernel
killed a render worker at 6.8 GB (Sep 14 22:41), 6.9 GB (22:45) and 5.4 GB (Sep 15 03:04),
each time with the 6 GB of swap exhausted. The first two are during the staging soak, which
reported 166 requests and no failures: each kill fell between requests and the container
restarted before the next one arrived. The soak measured time and never looked at memory.
The box has 8 GB and runs five engine containers, which idle at 4 GB together. Neither the
memory of a v2 render nor the memory of the box was checked before promoting. Both are mine.
Production is back on the old engine (`PET_ENGINE=v2` removed from its `.env`); staging is
down.

**Measured here, one 1600px preview, the hero dog:** peak 2.9 GB resident, and 2.1 GB still
held after the render returned with the caches at 3 MB and the Python heap at 31 MB. The
held memory is glibc: its mmap threshold grows as large arrays come and go, after which the
full-frame float arrays land on the heap and freed heap is never returned; `malloc_trim`
gave back 1.1 GB on demand. The peak is the composite stage: at "tonal match" 1.7 GB of
arrays were alive, two dozen full-frame colour planes from stages long finished, all held
by name in one function until it returned.

**Three changes, ink byte-identical (hero dog through the site path, lab through the engine
with landmarks, same hashes as before):**

- Every composite-stage array is released the line after its last reader (`del`; nothing
  computed differently). `edge` was computed for a blend whose weight has been 0.0 for
  weeks; it is released on the spot.
- The image sets `MALLOC_MMAP_THRESHOLD_=65536` (fixed, so every array over 64 KB is its own
  mapping and returns to the OS when freed), `MALLOC_TRIM_THRESHOLD_` and
  `MALLOC_ARENA_MAX=2`; the entry point calls `malloc_trim(0)` after each render.
- A memory cap on the container. A render that outgrows it kills that worker, which
  restarts in seconds; it can no longer take the box down. First put in the brand compose
  files, which turned out not to be what runs: every tree is started by `promote.sh` from
  the one `docker-compose.yml`, with the project, container name, port and image coming
  from the tree's `.env` (the brand files serve only the `./staging.sh`-style wrappers,
  which is why their `ps` printed an empty table). The cap lives in `docker-compose.yml`
  as `CONTAINER_MEM_LIMIT`, set to `3500m` in the staging and pawsinwords `.env`; unset,
  the default is above the box's RAM and the human-brand trees run as before. This
  Compose (v5.1.4) honored only the `deploy.resources.limits` form, so it is declared
  both ways. Checked with `docker inspect --format '{{.HostConfig.Memory}}'`.

**Measured after, seven renders in one process, four photos, three sizes, three aspects:**

    render 1  peak 2.2 GB  held after 1.0 GB
    render 2  peak 2.9 GB  held after 1.6 GB
    render 3-7  peak 2.85-2.93 GB  held after 1.59-1.62 GB   (plateau; +10 MB per render)

Before: peak 3.6 GB on the second render and 2.7 GB held, climbing. The 0.6 GB step between
the first and second render is not the caches (30 MB at their cap) and is not yet explained;
it is stable, and under the limit with 0.6 GB to spare at the worst peak seen. The next cut,
if one is needed, is the iteration stage (1.6 GB resident during placement, fields alive for
the whole loop) or a lower preview working height.

**The soak and the gate now watch memory.** `soak-petset.sh` samples the container's memory
through every request (`mem=` on each line; min/median/max and a flag over `MEM_MAX`, 3000 MB,
in the summary) and compares the container's restart count before and after, which catches a
kill no request saw. `render-petset.sh` does the restart-count check too. What this soak
would have said on the night of the 14th: `CONTAINER RESTARTED 2 time(s)`.

### The memory pass, second half: the arena

Staging on `f866677` with the cap: gate 8 identical and the lab moved by a twentieth of a
percent (cause pending the staging log), quick soak 22 requests, no failures, no restarts.
But the soak's new memory column put the shepherd (rendered at 1.3x for its small face) and
the concurrent pair at 3.49-3.50 GB, pressed against the 3.5 GB cap, and a 2400px print
measured 4.4 GB here, so a paid digital order's file would have been killed. Not production.

Two profiles found the rest. Between renders the process kept 0.66 GB more after its second
render than its first, with Python's own live objects at 51 MB: `mallinfo2` showed it in
malloc's mmapped chunks, i.e. native code, and the matting model in isolation reproduced it
exactly: onnxruntime's default CPU memory arena held 0.9 GB after ISNet's first run at
1024px and 1.95 GB after its second, for the life of the process. Arena off: nothing kept,
runs 2.2-3.2 s either way. The same setting now applies to the two landmark sessions
(rtmlib builds them without options, so the session is rebuilt without an arena after
load). During placement at print size, tracemalloc showed twenty named full-frame planes
(18 MB each at 2400) alive through the whole four-iteration loop, most of them the size
rule's intermediates already folded into `size_field`; released after the rule.

**Measured, MALLOC_* as in the image, ink byte-identical (hero dog site path, lab with
landmarks, same hashes as every previous pass):**

    preview 1600, seven renders   peak 1.39-1.56 GB   held after 0.34-0.42 GB
    print 2400                    peak 2.76 GB        held after 0.38 GB

    the same before this pass     preview 2.2-2.9 / 1.6 GB held   print 4.4 GB
    and before the whole memory work   preview 2.9-3.6 / 2.7 GB held, climbing

The container adds the landmark models (YOLOX 640, RTMPose 256, now arena-free too), about
0.3 GB. Preview about 1.8 GB and print about 3.1 GB under a 3.5 GB cap on the 8 GB box, with
the other four engines idling at about 1.3 GB beside it. Staging next: build, gate against
`f866677` (ink identical expected), quick soak, and the memory column is the number to read.

### Production, `d152923`, 2026-09-15 16:30 UTC

Staging on `d152923`: gate against `f866677` nine identical; quick soak 22 requests, no
failures, no restarts, container memory median 1876 MB, max 2887 MB (the concurrent pair),
shepherd at 1.3x 2500 MB. A digital order through Stripe test mode exercised the 2400px
print: file delivered, container peak 2.68 GB of the 3.42 GB limit, no restart. Then
pawsinwords: `PET_ENGINE=v2` and `PET_V2_VERBOSE=1` in its `.env`, `CONTAINER_MEM_LIMIT=3500m`,
promote, limit confirmed at 3670016000, staging taken down. First live render: landmarks
found, likeness 0.513 at the preview and 0.556 at print size, 22,289 words, no restart. The
box after: 2.2 GB used, 5.7 GB available, swap parked at 1.0 GB from the earlier builds.

Two things learned the hard way today, for the ops notes. The promote reads the compose
file of the tree it runs in, so every tree that is promoted must be checked out at the
commit whose compose file is wanted: the first production promote of the day ran with the
pawsinwords tree still on `751903e` and produced a container with no memory cap (caught by
the inspect line that is now part of the steps). And the trees had drifted: pawsinwords was
on the human-engine slider work, prod and staging on this branch. The container never sees
a tree's code, only its `.env` and compose file, so the render is unaffected, but the
drift is worth ending: one branch, three trees at the same commit.

Watch for a day: `docker inspect typortrait-pawsinwords --format '{{.RestartCount}}'` stays
0 and `journalctl -k --since today | grep -i 'out of memory'` stays empty. Open: the lab's
twentieth-of-a-percent move between `947b0c6` and `f866677` (staging log not yet read); a
two-pet photo and a person for the pet set; collisions at Large.

### Same input, two containers, same bytes

Two loupe renders of one photo, staging and production, came out close but not equal
(22,223 vs 22,245 words, likeness 0.553 vs 0.544). The lab from the pet set rendered through
the production container and gated against staging's run on the same image: byte-identical,
same numbers. The engine renders the same input the same way in both containers; the loupe
difference came from the inputs (the word line as typed, most likely). Two facts for the
notes: the gate can point at any container (`CONTAINER=... PORT=... NAME=...`), which is how
production is checked without touching an order; and the site's loupe asks for a print-size
render (2400px, `blur sigma=42.2` in the log), so every customer who opens the loupe runs the
print path, which is now the number to budget memory against, not the 1600px preview.

Checkpoint `checkpoint-2026-09-15`: production on `d152923` with the cap, v2 for previews and
prints, memory measured at every size, the soak watching memory and restarts.

### The pet set grows: two pets, a boy and a dog, a person (`10`, `11`, `12`)

Three photos added and rendered on staging (`d152923`): a tabby and a golden retriever side
by side, a boy with a Boston terrier, and a laughing woman from the human set. What the
detector said: two pets of comparable size, two faces read (eyes 261 and 160 px apart);
the terrier's face read and the boy not a subject; no cat or dog in the third, so the
customer note fires. Likeness 0.477 / 0.485 / 0.501.

**What the pictures showed, and what was measured before believing it.**

- The two-pet render looked as if a wedge were missing from the dog's head beside the cat
  and the cat's ear with it. The photo fetched here and the matte run exactly as the
  container runs it (1024px working height, the pixel budget having lowered it for the
  wide frame): one component, one 0.7% hole filled, both ears present. The "wedge" is the
  wall between the two heads, correctly backdrop. What reads as a bite is tone: the dog's
  ear beside the cat renders at L 75 against a source of 90, the same ratio as the rest of
  the coat, and the same with one face as with two (measured). The dog alone from the same
  pixels renders a little brighter and scores 0.478 against 0.457 in the pair. The two-pet
  path itself changes nothing about the coat; both faces are built.
- The cat's nose was an orange patch on the fur above the real nose. `locate_nose` refines
  the landmark nose with a dark-blob fit, and a cat's nose is pink and lighter than its
  fur: the fit slid 0.14 eye-separations up the bridge onto a stripe, 2.4 times longer
  than wide. With a landmark nose the fit is now only accepted within 0.12 eye-separations
  of it, at most twice as long as wide, between 0.06 and 0.40 eye-separations across;
  otherwise a plain nose-sized ellipse sits at the landmark. The dog's fit (drift 0.08,
  aspect 1.1) is unchanged; the cat's reveal is on its nose. Ink on the hero dog (no
  landmarks) is byte-identical. The local lab render moved, and looking at why exposed
  that the lab coordinates used in every local landmark test since the eye work
  (`309,226;496,212;402,330`) put the "nose" on the bridge between the eyes, not on the
  leather: a hand-typed stand-in, never the pose model's output. The staging gate, which
  has the real landmarks, is the judgment on this change for the nine.
- The boy: the terrier is built properly, the boy reads as a boy but his eyes are dark
  smudges, since no eye pass runs on a face the pet detector does not know. The image
  already carries the human face detector for the other brands; a person's eyes could get
  the same reveal. The woman alone: rendered as a pet would be, note shown, likeness 0.50;
  the better answer later is to hand a no-pet photo to the human engine.

The first form of the nose guard, distance from the landmark, went through the staging
gate (`833bdd0`) and failed it: four of nine dogs moved, three for the worse, the lab by
0.011. The refined fit on a dog legitimately sits further from the nose-TIP keypoint than
the rule allowed, because a big leather's centre is below the tip. A second form, "is the
nose darker than the fur round it", did not separate the cat either: a disk round its nose
takes in its stripes. The form that holds is direction: a real leather's centre is at or
below the tip along the line from between the eyes to the nose, and the cat's bad fit lay
0.09 eye-separations above it. Fits above the landmark, or slivers over 3:1, take the plain
ellipse; every other fit stands. The golden retriever (0.08 below) and the terrier keep
theirs. Gate `d152923` → this build should read nine identical and the cat-and-dog moved.

Gate `d152923` → `01aa77f`: eleven identical, the cat-and-dog moved, likeness 0.4765 →
0.4565. The picture: the cat's nose is a small pink nose where the cat's nose is, the eyes
green and lit, fine type on both faces, the dog unchanged. The score dropped because the
old fit drew a large nose outline on the dark stripes of the bridge, and dark ink on a dark
stripe scores well against the blurred photo; the metric rewarded a mistake. The picture
stands; `01aa77f` is the baseline for the twelve, with the pair's score recorded as 0.4565.

What the pair still shows, for later: the cat renders greyer and flatter than a tabby
alone would. The tonal match is one quantile map over the whole silhouette, so a grey
cat and a golden dog are matched as one coat. A per-subject match, each animal's region
against its own pixels, is the next step for two-pet portraits when they matter.

### A person is a face too

The boy beside the terrier, on production: one coarse word size wallpapered across his
face, no eyes. The pet detector does not know a human face, so he got the far-body
treatment. The image already carries the MediaPipe face mesh the human brands use (478
points, iris centres, nose tip, lips). `pet_landmarks.human_faces` reads every person in
the frame; a face whose eyes lie inside a cat or dog's box is that animal (MediaPipe calls
the golden retriever a human face, eyes 256 px apart, measured) and is dropped. A person
of at least half the largest pet's eye separation, or anyone when there is no pet, becomes
a subject: the landmark string carries the face as "h:eyes;nose;mouth", and the engine
gives it the eye passes, the eye reveal, the fine zone (eyes, a nose-sized ellipse at the
nose tip, the mouth where the mesh puts it) and a place in the size rule and the likeness
weight, and does NOT draw the nose leather, the mouth crease or the whiskers. With no pet
in the photo the person is the primary face, hero words and all, and the customer note
now says the portrait was built around the face that was found.

Local, the boy with the terrier (terrier from staging's landmarks, the boy from the
mesh): eyes revealed, fine type on his features, likeness 0.467. The boy alone, primary:
0.453. The nine single pets: a human face is only looked for after the pet detector has
run, and dropped inside any pet box, so their renders do not change (hero dog identical
here; the gate is the proof for the nine). MediaPipe needs the GL libraries; the image has
them (the human brands run on it), this sandbox needed `libgles2`.

### The three open items

**Collisions at Large.** Per-pass accounting added to the report card ("collisions by pass"):
the structural pass makes three quarters of every colliding pixel at every slider setting,
and its own body-overlap rate climbs with the slider (hero dog 0.99% at Small, 1.18% at
Large). The per-word overlap cap was a fixed fraction of the word's own pixels, so a word
1.6x larger was allowed 1.6x the ink on its neighbour. The cap now shrinks with the slider
(`GOP_MAX_OVERLAP / type factor`), so Small is unchanged (byte-identical) and Large
tightens. Measured at Large: hero dog 0.97% -> 0.46%, lab on a landscape canvas 0.63%, cat
0.61%, all under the 1.20% claim line; exposed space 0.9-1.6%, under the 3.0% line.

**Two coats, one match.** With two or more faces the letter, gap and saturation quantile
maps are taken per animal (a Voronoi split on the eye midpoints, blended over half an
eye-separation at the seam). Measured on the cat and dog: the cat's saturation 71 -> 78
against a source of 81; the dog unchanged (133 against 128). Luminance moves little,
because the fraction every coat is darkened by is a design constant, not a per-animal
matter. Single pets run the maps exactly as before (byte-identical). A person beside a pet
now also gets its own maps, skin against coat, so the boy-and-dog photo moves in the gate.

**The lab's move, 947b0c6 -> f866677.** Staging shows one landmark line for the lab and no
"comparable size" line: one subject, so not a second face. That leaves the re-seeding of
unreached streamline territory, which e85f6f9 added. The per-iteration log line now says
"(N re-seeded, +M)" so the next staging render of the lab settles it without a debug flag.

Staging, `81a29cf`: gate against `6f356e7` ten identical, the two two-face photos moved in
bytes with every number unchanged (colour maps only). Large, the three photos the soak had
over the line: samoyed 0.53%, shepherd 0.58%, lab 0.43% (were 1.23, 1.38, 1.70). The
re-seed count in the log: one re-seed in the whole set, three lines, at one photo's second
iteration; that is the lab's move between `947b0c6` and `f866677`, a corner of its
silhouette the growth wave had not reached. Nothing on the open list is open.

### A third ground: the photo's own

The engine has read the photo's background colour since the first week (a masked average
outside the animal, desaturated and lifted), and no customer ever saw it, because the page
always sends a gallery ground and that choice wins. Now a third choice, "Photo": the
entry point takes the median LAB colour of every pixel the matte calls background, on the
source before print fitting, and uses it as the ground; None when under 2% of the frame is
background, in which case the gallery ground stands. The colour travels with the cached
render, so switching to Photo on a portrait already rendered is the same instant arithmetic
as the other two grounds (measured: 0.0 s). Gallery Dark and Gray are untouched (hero dog
byte-identical). Measured grounds: hero dog (61, 80, 33), its garden; the cat (169, 147,
123); the cat-and-dog (130, 127, 122), the wall. Default stays Gallery Dark: a light ground
behind a light coat loses the silhouette edge, and the pale whisker and fringe hairs vanish
on it, so this is a choice the preview shows, not a default.

Staging `15971de`: gate twelve identical; the samoyed on its own sky holds its silhouette
and the fringe reads as fluff, so the light-coat worry is about pale walls, not light
backgrounds as such. Production on `15971de`. When a photo has under 2% background (a tight
crop) and Photo is chosen, the customer now gets a note that the portrait is on Gallery
Dark because there was no colour to borrow; measured on the hero dog's face cropped to
0.7% background, on the fresh render and on the cached one, and silent on the other grounds.

### The full soak, `c398ab4`, twelve photos

220 requests: twelve photos x three sizes x three aspects x two grounds plus the concurrent
cases. No failures, none over 90 s (fresh renders 10 / 29 / 67 s, the 67 the second of two
queued), no collisions over 1.20% at any size or aspect (the Large row that failed on
`947b0c6` is clear), exposed space under 3% everywhere, container restarts 0. Memory,
peak during a request: median 1699 MB, max 3275 MB. Four requests over the 3000 MB flag:
the shepherd on a landscape canvas at Small (3023) and at Large (3271), the full-body photo
the engine renders at 1.3x for its face on the widest canvas the site offers, and the two
queued renders (3212, 3275). The cap is 3500 MB, so the worst case has 230 MB in hand; the
cap kills a worker and not the box, so the failure shape, should it come, is one customer's
render and a restart in seconds. The margin is the number to widen next, either by cutting
the placement stage's resident planes further or by the memory upgrade.

### 2026-09-16: Woven, the pet engine pointed at a person (staging only)

Assessment of the "Typographic Portrait Research Framework" (its report and two
prototypes): its baseline is wrong, the human sites' Displacement style already tiers type
by landmark masks and anchors eyes and lips from the 478-point mesh; the stack it proposes,
streamlines on a field with zone-weighted spacing and a likeness measure, is the pet v2
engine; the one transferable idea is direction, since Displacement's rows are horizontal
and warped by luminance and nothing follows the jaw or the brow; and its legibility model
and deformation stage are the opposite of these products, which never bend a letter.

Built, all of it revertible and none of it reachable from production:

- `pet_landmarks.anatomy_field`: the face oval, both brows, both eye rings, the lips and
  the nose traced as chains from the mesh's own connection sets; every pixel takes the
  tangent of the nearest point on the nearest chain, weight 1 on a contour falling to 0 by
  1.5 eye-separations. `render_v2(anatomy=...)` blends it with the texture field in
  doubled-angle space, so on skin the lanes run with the structure and in the hair with
  the texture. `anatomy=None` (every animal) is byte-identical (hero dog and lab hashes).
- `render_pet_portrait_v2(subject="human")`: no pet is looked for, the largest person is
  the primary face, the anatomy field is built from their mesh, the "no cat or dog" note
  is not raised. Part of the cache key.
- main.py: a request with `style=woven` renders that way only when the tree's `.env` has
  `TYPO_WOVEN=1` (`config.WOVEN_ENABLED`); otherwise it renders Displacement. The recipe
  carries `woven` so the paid file matches the preview. The page sends `style=woven` for a
  human brand only with `?woven=1` in the address; no control a customer sees changes.
- `render-petset.sh MODE=woven` gates a folder of people the same way as the pet set.

Measured here on the boy alone (the only person on this disk), 1600px: as the pet site
would render a person with no detector, likeness 0.429; Woven, eyes revealed and the
lanes bending round the features, 0.429. The score does not separate them and the
pictures do, which is the point of the blind test: the same photos through Displacement
and through Woven, unlabeled, "which looks more like the photo". That test, not the
score, decides whether Woven becomes a style on Typortrait.

**Woven on staging, first look, and skin.** Gate `c398ab4` → `df3bef9`: twelve pets identical,
twelve people through Displacement identical (`compare-testset.sh human-c398ab4
human-df3bef9`). Three people through Woven: sidelight 0.455, dark skin 0.673, smile 0.501,
each with a face and an anatomy field in the log. The smile pair, Displacement beside
Woven: Displacement is the photograph with the passage as a fine texture, likeness as high
as it gets, words not readable; Woven is a portrait built of readable words, the lanes
running along the cheeks and round the mouth, likeness at a remove, and the whole face
orange. The cast measured on the boy's face: L 92 against a source of 130, saturation 112
against 90. Cause: the tonal reference was the whole silhouette, mostly a navy hoodie, mean
L 87, so the engine chose its dark-coat policy for a face at 130; and the saturation
quantile map ran over hair, clothes and skin as one distribution. For a human subject the
face ellipse is now the reference for the coat-mode decision, the letter and gap samples
and the saturation gain (a single mean gain instead of the map), the tonal maps run per
region, face against the rest, and the gap layer keeps more of the skin's value (0.55
against 0.40 of source, saturation 0.75 against 0.85). Measured after: face L 102, S 98;
the skin reads as skin. Every one of these sits behind the human flag; pets byte-identical.

Staging `b5c50ae`: Displacement's twelve identical ("the runs are the same render"), the
pets' twelve identical, the three Woven renders unchanged in every number (the skin policy
is colour, not typography). The smile in this version: skin as skin, the laugh and the eyes
hers, the words readable along the cheeks and the jaw; the neck and chest run a little
warmer than the face, since everything below the face ellipse is matched with the hair and
the blouse as one region -- a follow-up if the blind test gives Woven a future. This is the
version for the blind test: three photos, Displacement beside Woven, unlabeled, two
questions kept apart -- which looks more like the photo, which would you rather have on
the wall. Staging down afterwards; nothing on any live site has changed.

**Woven, the crispness pass.** Seen on the staging page beside Displacement, Woven was soft
and darker, and the softness is structural: in this engine a letter reveals the photo and
the gaps are a blurred, darkened copy of it, a recipe for fur. For a human subject, four
knobs, none reaching an animal or Displacement: the gap layer is barely blurred
(sigma 0.0012 W against 0.006 W), so the photo's detail sits between the letters; the gaps
keep 0.70 of the skin's value (0.55 before, 0.40 for fur); tone fidelity 1.0 and the
letter-over-gap floor 24 L; every structural letter at full ink. And the Woven route
takes the page's own ground (paper, navy, black), so a pair can be compared on the same
ground. Boy's face: L 106 (was 92 at first, 102 after the skin pass) against 130, S 98
against 90; the eyes and the mouth are his. Pets byte-identical.

**Woven takes the backdrop chip.** On Typortrait the page's chips (Studio, Sand, Cutout)
are the backdrop axis; Woven read only the ink ground, always navy on that brand, so the
chips did nothing. A chosen chip is now the ground, as the explicit colour from
Displacement's own table (`ground_override`), on the preview and the paid file alike;
Cutout and the florals have no colour and fall back to the ink ground. Default ground
byte-identical; sand paints (208, 228, 238).

**Woven, the wisps.** Beside Displacement on the same light ground, Woven lost the flyaway
hair: Displacement feathers the silhouette, Woven paints the ground up to the matte and
reveals the photo only through letters, and no letter is laid on a strand a few pixels
wide. For a person the soft edge of the matte (under 0.3) now reveals the photo at the
matte's own opacity, strand for strand; inside the solid silhouette nothing changes; pets
byte-identical. Also noted: Typortrait's chips are all light grounds, so navy is only
Woven's fallback there, and the pair now sits on the same ground.

**Woven, the wisps, measured.** "Can't tell if there is an improvement" -- because there
was none to see. The previous pass revealed the photo in the soft edge of the matte, and
the matte has no strands in it: the raw ISNet matte reaches 0.6 nowhere outside the solid
silhouette on the boy, and the solidified matte's edge is a blurred outline, so the reveal
painted a 10px brown smear round the head (band mean 42 L off the ground, 98% of the band
pixels "visible", none of them hair). Displacement's strands come from somewhere else: its
silhouette is refined with a guided filter that snaps the soft mask onto the photo's own
luminance edges (app/pipeline/silhouette.py, `_soft_matte`).

The human path now builds that same fringe matte (`_wisp_matte`: the raw matte through
Displacement's guided filter, same radius, confined to a band round the solid silhouette),
and outside the solid silhouette the pixel is the outer ground with the photo over it at
the fringe's alpha -- the way Displacement composites its edge. The old reveal is gone.
Two calibrations, both measured outside the solid silhouette on the boy:

    knee                    px over 0.3   2px ring mean   seen
    Displacement's 0.12/1.5           5           0.028   crisp edge, tufts, no strands
    none                           1130           0.137
    0.05 / gamma 0.6               6041           0.205   strands, but a tan feather round the whole outline
    gamma 0.5                     14975           0.324   haze
    0.05 / 0.6, edge-gated         1647           0.139   strands and tufts, thin feather

The gate: a strand is an edge in the photo and the soft ring is not, so the alpha is
scaled by the luminance gradient (a flat pixel keeps a fifth, a hair edge all of it).
Band numbers, boy on Sand: smear 41.7 L -> 5.8 with Displacement's knee -> 17.3 gated,
with the difference now hair. Honest limit: ISNet resolves this boy's tufts, not his
finest flyaway hairs; those are faint in the matte and stay faint. The backdrop swap
(`outside_w`) follows the fringe, so the chips remain exact. Pet hashes unchanged
(f1e262315176, af6953c9a33d); the server gates say the rest.

**Woven, the wisps, traced to their source.** Staging `03232c5`: Woven moved on all
three, Displacement's twelve identical, and still "no strands" at her hairline on Sand.
Both mattes dumped from inside the container at her crown (photo, Woven fringe,
Displacement's matte, raw model output): the hairline is smooth against the studio wall
and every matte agrees on it. There was nothing at that edge for either engine to keep.
The strands she shows in Displacement are the hair's own texture, at the sides and inside
the mass, which Displacement keeps because its letters are a warp of the photo and the
photo is everywhere. In Woven a letter reveals the photo and a gap is a darkened copy, so
the structure is there but the letters' edges are what the eye reads. Measured on the boy
(high-pass L in the hair, correlation with the source's): Woven carries 0.33 of the strand
structure. A hair reveal (the photo through everything outside the face ellipse,
`PET_V2_HAIR_REVEAL`) took that to 0.35 / 0.36 / 0.39 at 0.35 / 0.5 / 0.7, and the eye saw
little on the boy, whose photo is 640px wide and upscaled. Off by default, passed through
by the staging compose, to be switched on for her photo and judged there. Pet hashes
unchanged. Also seen in her Displacement loupe: blue speckle along the hair against the
cheeks on both sides, a Displacement defect that a blind pair would show.

**Woven, the size across the jaw.** Her loupe: the chin's smallest words beside the
neck's largest. Not the slider (it scales both ends together); two rules meeting at the
jaw. Size grows with distance from the nearest feature and the ramp finished 1.2
eye-separations out, which on a laughing face is the chin; and smoothness carried 0.40
of the size, so a neck, the smoothest skin in the frame, was read as far body and given
the biggest words in the portrait. The gap-fill then dropped words at 0.55 of the local
size between them. Four changes for a person, none reaching an animal: the range is
three to one (0.30 base against 0.52), the near ramp runs 2.0 eye-separations, smoothness
carries 0.15, the fills sit at 0.70 of the local size. Measured on the boy in the band
below the mouth (0.3-2.0 eye-separations), nearest-neighbour size ratio and band range:

    build     words   ratio p50   p95    max   band p05-p95      range
    before     1063        1.33  3.82   7.33   6.0 - 23.0 px     3.83x
    after      1422        1.18  2.17   3.00   6.0 - 14.0 px     2.33x

Hair reveal 0.7 on for both. Pet hashes unchanged. The hair: the reveal at 0.7 on her
photo is the closest Woven has come to Displacement's hair, and she settled it for now.

**Woven, the missing sleeve.** A woman in a beige cardigan against a beige wall, uploaded
through the page (now `woven/src/H15.png`): Woven painted the right third of her, sleeve
and shoulder, as ground. Dumped from the container: the matte model gave one piece and
55.5% of the lower-right region; the human pipeline's segmenter (MediaPipe, guided
filter, what every live human site is cut with) gave 86.4%. Not the largest-piece rule,
then: the model never saw the sleeve. For a person the raw matte is now the union with
that silhouette (`_with_person_silhouette`), fail-safe, before solidifying, fitting and
the fringe. Boy: matte 84.2% of the frame, union 85.8%; jaw band unchanged in kind
(p95 2.17, range 2.17x). Pet hashes unchanged.
Staging `3897a9c`: pets twelve identical, Displacement's twelve identical. Woven: her
right shoulder and sleeve are back (union 66.2% -> 71.6% of the frame); the three older
photos gained 0.0-0.1% and reshuffled, and 01-sidelight's likeness fell 0.011, one
thousandth past the line -- looked at side by side, the same silhouette with the words
in different places, the dark side of the face as it was. Filed as layout noise, not
regression. This is the build for the blind test.

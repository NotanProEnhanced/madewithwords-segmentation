fastapi==0.136.1
uvicorn==0.47.0
python-multipart==0.0.29
numpy==2.4.6
pillow==12.2.0
opencv-contrib-python==4.13.0.92
cairosvg==2.9.0
svgwrite==1.4.3
mediapipe==0.10.35
qrcode==8.2
stripe==15.1.0
httpx==0.27.2
# --- Pet / non-human subject support (experimental, feat/pet-support) --------
# General foreground matte for pets/objects. onnxruntime is the CPU inference
# backend. NOTE: rembg pulls opencv-python-headless; this repo already uses
# opencv-contrib-python — if the build/import conflicts, reconcile to a single
# OpenCV (e.g. install rembg with --no-deps + its needed deps). Validate on
# staging before promoting. Only imported when a non-person subject renders.
rembg==2.0.59
onnxruntime==1.19.2
